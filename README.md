# LIFF and Bot

liffとLINE Botのサンプルテンプレート

``` sh
npm install
npm run dev
```

``` sh
open http://localhost:3000
```

## `.env` sample

```text
LIFF_ID=<一度LIFFを発行してからコピペ>
CHANNEL_SECRET=<LINEからコピペ>
CHANNEL_ACCESS_TOKEN=<LINEからコピペ>
```
